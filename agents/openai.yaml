interface:
  display_name: "macOS Codex Remote Update"
  short_description: "Update macOS Codex remotely"
  default_prompt: "Use $codex-remote-update to update the local macOS Codex app and verify it relaunched."
